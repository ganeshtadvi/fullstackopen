```mermaid
sequenceDiagram

    participant browser
    participant server

    Note over browser,server: user visits the url "https://studies.cs.helsinki.fi/exampleapp/spa" then write note inside input
    Note over browser, server: main.js file execute after user clicks on save button.

    browser->>browser: Browser doesn't reload 
    browser->>browser: Create new note with Content + date
    browser->>browser: add note in local list then render it immediately

    Note right of browser: browser send data to server using AJAX 

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa  
    activate server
    server-->>browser 201 Created (Note Saved in json data at server)
    deactivate server


    Note right to browser: browser keeps showing updated list without reploading current url.


```
